[project]
name = "lain42"
version = "0.1.0"
description = "Configuration sharing platform — Pinterest for dotfiles"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.136.3",
    "grpcio>=1.81.0",
    "grpcio-tools>=1.81.0",
]
